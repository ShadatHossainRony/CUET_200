<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
      background: linear-gradient(135deg, #ff6b6b 0%, #ee5a6f 100%);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 20px;
    }

    .container {
      background: white;
      border-radius: 16px;
      box-shadow: 0 20px 60px rgba(0,0,0,0.3);
      max-width: 400px;
      width: 100%;
      padding: 40px 30px;
      text-align: center;
    }

    .error-icon {
      width: 80px;
      height: 80px;
      background: linear-gradient(135deg, #ff6b6b 0%, #ee5a6f 100%);
      border-radius: 50%;
      display: flex;
      justify-content: center;
      align-items: center;
      margin: 0 auto 20px;
      font-size: 48px;
      color: white;
    }

    h1 {
      color: #333;
      font-size: 24px;
      margin-bottom: 10px;
    }

    p {
      color: #666;
      font-size: 16px;
      margin-bottom: 20px;
      line-height: 1.6;
    }

    .error-details {
      background: #fff5f5;
      border-left: 4px solid #ff6b6b;
      padding: 15px;
      margin: 20px 0;
      text-align: left;
      border-radius: 4px;
    }

    .error-label {
      color: #999;
      font-size: 12px;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 5px;
    }

    .error-value {
      color: #ff6b6b;
      font-weight: 600;
      font-size: 14px;
    }

    .details {
      background: #f7f9fc;
      border-radius: 8px;
      padding: 20px;
      margin: 20px 0;
      text-align: left;
    }

    .detail-row {
      display: flex;
      justify-content: space-between;
      padding: 10px 0;
      border-bottom: 1px solid #e1e8ed;
    }

    .detail-row:last-child {
      border-bottom: none;
    }

    .detail-label {
      color: #999;
      font-size: 14px;
    }

    .detail-value {
      color: #333;
      font-weight: 600;
      font-size: 14px;
    }

    .btn {
      display: inline-block;
      padding: 12px 32px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      text-decoration: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 600;
      margin-top: 10px;
      transition: transform 0.2s;
    }

    .btn:hover {
      transform: translateY(-2px);
    }

    .help {
      margin-top: 30px;
      padding: 15px;
      background: #f7f9fc;
      border-radius: 8px;
      font-size: 13px;
      color: #666;
    }

    .help strong {
      color: #333;
      display: block;
      margin-bottom: 5px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="error-icon">✕</div>
    
    <h1><%= title %></h1>
    <p><%= message %></p>

    <% if (reason) { %>
    <div class="error-details">
      <div class="error-label">Error Reason</div>
      <div class="error-value"><%= reason %></div>
    </div>
    <% } %>

    <% if (error) { %>
    <div class="error-details">
      <div class="error-label">Technical Details</div>
      <div class="error-value"><%= error %></div>
    </div>
    <% } %>

    <% if (transaction_id) { %>
    <div class="details">
      <div class="detail-row">
        <span class="detail-label">Transaction ID</span>
        <span class="detail-value"><%= transaction_id %></span>
      </div>
      <% if (amount) { %>
      <div class="detail-row">
        <span class="detail-label">Amount</span>
        <span class="detail-value">৳<%= amount.toFixed(2) %></span>
      </div>
      <% } %>
      <% if (balance !== undefined) { %>
      <div class="detail-row">
        <span class="detail-label">Current Balance</span>
        <span class="detail-value">৳<%= balance.toFixed(2) %></span>
      </div>
      <% } %>
    </div>

    <a href="/wallet/pay/<%= transaction_id %>" class="btn">Try Again</a>
    <% } %>

    <div class="help">
      <strong>Common Issues:</strong>
      • Insufficient balance - Topup your wallet<br>
      • Invalid PIN - Check your PIN and try again<br>
      • Expired transaction - Request a new payment link<br>
      • Account inactive - Contact support
    </div>
  </div>
</body>
</html>
